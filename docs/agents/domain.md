# Domain Docs

BioassayCraft uses a single-context domain documentation layout.

Primary project context lives in the root documentation set under `docs/`. Architecture decisions, when added, should live under `docs/adr/`.

Agents should read `AGENTS.md`, `docs/design-system.md`, `docs/interaction-system.md`, and the relevant domain docs before changing user-facing scientific interaction pages.
